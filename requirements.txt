# ============================================================
# Dependências de RUNTIME (instaladas na imagem Docker de produção).
# Versões fixadas (pins) conforme a suíte de testes validada localmente.
# ============================================================

# Web framework e servidor WSGI de produção.
flask==3.1.3
gunicorn==22.0.0

# Extração de texto/PDFs (holerites).
pypdf==6.16.2
pdfplumber==0.11.10

# Integração com a API DeepSeek (parsing universal).
requests==2.34.2

# Manipulação de dados e exportação (XLSX/CSV).
pandas==3.0.5
openpyxl==3.1.5

# Geração de dossiê em PDF (ReportLab).
reportlab==5.0.1

# Carregamento de variáveis de ambiente a partir de `.env`.
python-dotenv==1.0.1

